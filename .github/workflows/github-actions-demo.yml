on: ["push"]
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to count cigarett
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Count pollution cigarett
        uses: "./" # Uses an action in the root directory
        id: cigarettId
      - name: echo the pm25 in delhi
        run: echo "The pm25 at delhi right now is ${{steps.cigarettId.outputs.noOfcigarette}}"
      - name: run npm
        run: npm i
      - name: Commit files
        run: |
          git config user.name shubham-sharma
          git config user.email shbh541@gmail.com
          git add .
          git commit -m "generated"
          git push
